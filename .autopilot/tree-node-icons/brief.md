# Бриф: иконки узлов дерева профилей (группы, папки, развёрнутые узлы)

Пользователь (перевод требования с русского, verbatim после редактирования):

> Расширь ProtocolIcon: добавь иконки для групп, папок и развёрнутых узлов дерева профилей.

Контекст: серия UI-правок (иконки — `.autopilot/ui-icons/`, контраст — `.autopilot/ui-contrast/`). Этот полёт переносит узлы дерева профилей с текстовых глифов на SVG-иконки.
