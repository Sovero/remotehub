# 04 — Интеграция: App.tsx, история, статусбар, очистка

**Требования:** R04.2, R04.3, R11i, R11i.1, R11i.2, R06, R07, R09i
**Blocked by:** 02, 03
**Status:** ready

## Что должно заработать

RDP-вкладка полностью интегрирована: при обрыве соединения — сообщение об ошибке
и кнопка «Переподключиться», при неверном пароле — понятный текст. История
подключений пишется автоматически (как для SSH). Статусбар показывает адрес
активного RDP-сервера. Старый mstsc-код отключён: `RdpManager.launch()` больше
не вызывается, `win32-engine.ts` не загружается.

## Из брифа, дословно

> «внешних окон mstsc нет»
> «панель подключения (BBAR) не появляется»

## Разделы спеки

Истории 12, 13, 14, 15. Решения: Существующие фичи, Модули (старый код).

## Критерии приёмки

- [ ] `App.tsx`: RDP-вкладка рендерит `RdpCanvas` вместо `rdp-stage` заглушки
- [ ] Обрыв соединения → ошибка «Соединение с RDP-сервером потеряно» + кнопка «Переподключиться»
- [ ] Неверный пароль → ошибка «Неверное имя пользователя или пароль», вкладка остаётся
- [ ] `HistoryEntry` создаётся при подключении, закрывается при дисконнекте
- [ ] Статусбар: «RDP: 10.10.51.2:3389» для активной вкладки
- [ ] `RdpManager.launch()` не вызывается — весь RDP идёт через `RdpIronManager`
- [ ] `win32-engine.ts` не загружается при старте приложения (koffi остаётся для VNC)
- [ ] `src/main/rdp/rdpjs-client.ts` переименован/удалён (заменён на iron-manager)
- [ ] `tests/rdp-embed.test.ts`: старые тесты mstsc адаптированы к IronRDP (или заменены)
- [ ] Полный цикл: двойной клик по хосту → вкладка → рабочий стол → закрытие → история