# Бриф: иконки SFTP вместо текстовых глифов

Пользователь (перевод требования с русского, verbatim после редактирования):

> Замени текстовые глифы в SFTP на иконки: файл/папка (▸/▢) и направление передач (↑/↓) — SVG из набора Icon.

Контекст: серия UI-правок иконок (`.autopilot/ui-icons/`, `spinner-states/`, `ui-contrast/`, `tree-node-icons/`). Этот полёт убирает последние текстовые глифы из панели SFTP.
