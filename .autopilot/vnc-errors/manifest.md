# Манифест требований

Источник: `brief.md`. Строку из этого списка может снять **только пользователь**.

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R01 | «понятные сообщения об ошибках в VNC-вкладку… когда сервер не отвечает на рукопожатие» | done | Диагностировано эмпирически: noVNC при молчащем сервере висит в «Подключение…» без объяснений; мост теперь следит за рукопожатием и при отсутствии ответа (10 с) шлёт в рендерер сообщение «VNC-сервер не отвечает на рукопожатие…»; сверху фолбэк-таймаут вкладки (15 с) | spec → T01 |
| R02 | «…или требует не поддерживаемый тип шифрования» | done | Мост разбирает секцию security (3.3/3.6 — один байт, 3.7+ — count+types) и при отсутствии поддерживаемых типов (None/VNC/TLS и др.) шлёт сообщение с перечнем типов и советом включить авторизацию без TLS | spec → T01 |
| R01.1 | *(углубление)* сообщение приходит из main в рендерер по каналу `vnc:error` и не перезаписывается фолбэком | done | ASSUMPTION — детерминированное сообщение моста ставит фазу error; таймаут вьювера срабатывает только если фаза всё ещё connecting | spec → T01 |
| R01.2 | *(углубление)* вкладка не висит в «Подключение…» вечно ни при каком молчащем сервере | done | ASSUMPTION — фолбэк-таймаут 15 с в `VncViewer` ставит error, если мост по какой-то причине не сработал | spec → T01 |
| A01 | *(дополнение)* найден и устранён скрытый баг: `rfb.connect()` в VncViewer — метода нет в noVNC 1.7 (конструктор сам начинает подключение) | done | ASSUMPTION — вызов падал на каждой VNC-сессии и молча ронял вкладку в error с текстом «rfb.connect is not a function»; смоук happy-path проверял только пиксели канваса и не замечал оверлей. Убрано; смоук усилен проверкой отсутствия оверлея | spec → T01 |
| A02 | *(дополнение)* покрытие вотчера тестами | done | ASSUMPTION — 4 теста: молчащий сервер, не-RFB-ответ, неподдерживаемые security-типы, happy path (security без ошибки) | spec → T01 |
