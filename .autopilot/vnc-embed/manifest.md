# Манифест требований

Источник: `brief.md`. Строку из этого списка может снять **только пользователь**.

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R01 | «VNC-протокол тоже открывается в отдельном окне» | done | ASSUMPTION — диагностировано эмпирически: VNC-вьювер (noVNC) уже встроен во вкладку, но мост терял ранний баннер RFB-сервера («RFB 003.008\n» отправляется сразу после TCP-connect, до подключения WebSocket-клиента noVNC) — рукопожатие зависало навсегда, канвас оставался чёрным; пользователь видел VNC «не внутри» приложения. Исправлено: мост буферизует ранние байты и отдаёт их при подключении WebSocket | spec → T01 |
| R01.1 | «встрой его внутрь приложения по аналогии с RDP» | done | ASSUMPTION — VNC рендерится прямо во вкладке (noVNC canvas); проверено против реального RFB-рукопожатия (fake-сервер 3.8): кадр отрисован, все выборки пикселей цветные — и в dev, и в packaged-сборке | spec → T01 |
| A01 | *(дополнение)* гонка ранних байтов покрыта регрессионным тестом | done | ASSUMPTION — тест «буферизует ранние байты сервера до подключения WebSocket-клиента» в `tests/vnc-bridge.test.ts` | spec → T01 |
